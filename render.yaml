# NOTE: Render discontinued free web services
# Use this for paid hosting, or see alternatives below

services:
  # Backend API Service (Starter: $7/month)
  - type: web
    name: task-api-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"

# Frontend: Deploy separately as Static Site (free on other platforms)
# See README for free hosting alternatives
